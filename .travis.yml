sudo: required

language: bash

services:
  - docker

before_install:
  - docker pull koalaman/shellcheck

script:
 - docker run -v $(pwd):/scripts koalaman/shellcheck /scripts/glsysbackup ; exit $?

matrix:
  fast_finish: true
